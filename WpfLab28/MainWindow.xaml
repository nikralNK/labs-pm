<Window x:Class="WpfLab28.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Лабораторная работа 28 - WPF"
        Width="800"
        Height="500"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="красный" Width="100" Height="40" Margin="0,0,10,0" Click="BtnRed_Click"/>
            <Button Content="зеленый" Width="100" Height="40" Margin="0,0,10,0" Click="BtnGreen_Click"/>
            <Button Content="синий" Width="100" Height="40" Margin="0,0,10,0" Click="BtnBlue_Click"/>
            <Button Content="желтый" Width="100" Height="40" Click="BtnYellow_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="привет" Width="100" Height="40" Margin="0,0,10,0" Click="BtnHello_Click"/>
            <Button Content="до свидания" Width="120" Height="40" Click="BtnGoodbye_Click"/>
        </StackPanel>

        <TextBlock Grid.Row="2" x:Name="lblMessage" FontSize="14" FontWeight="Bold" Margin="0,0,0,20"/>

        <GroupBox Grid.Row="3" Header="Задание 2: Вычисление функций" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                    <TextBlock Text="X:" VerticalAlignment="Center" Width="20"/>
                    <TextBox x:Name="txtX" Width="100" Margin="5,0,20,0"/>
                    <TextBlock Text="Y:" VerticalAlignment="Center" Width="20"/>
                    <TextBox x:Name="txtY" Width="100" Margin="5,0,20,0"/>
                    <TextBlock Text="Z:" VerticalAlignment="Center" Width="20"/>
                    <TextBox x:Name="txtZ" Width="100" Margin="5,0,20,0"/>
                    <Button Content="Вычислить" Width="100" Height="30" Click="BtnCalculate_Click"/>
                </StackPanel>

                <TextBlock Grid.Row="1" x:Name="lblResult1" FontSize="12" Margin="0,5"/>
                <TextBlock Grid.Row="2" x:Name="lblResult2" FontSize="12" Margin="0,5"/>
                <TextBlock Grid.Row="3" x:Name="lblError" Foreground="Red" FontSize="12" Margin="0,10"/>
            </Grid>
        </GroupBox>
    </Grid>

</Window>
